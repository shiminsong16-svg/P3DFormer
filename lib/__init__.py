"""Top-level lib package initializer.

This file makes the `lib/` directory a proper Python package so that
imports like `from lib.attention_rpe_ops.functions import attention_rpe_ops`
resolve correctly when running tools inside this repository.
"""
